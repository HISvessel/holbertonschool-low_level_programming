#include "main.h"
/**
 * _abs - finding the absolute value.
 *@n: integer for abolute value
 *Return: Absolute value of an integer
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
