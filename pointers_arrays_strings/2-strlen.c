#include "main.h"
/**
 * _strlen - this function prints out the string length
 *@s: char to measure
 *Return: length
 */
int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
		return (len);

}
